[request_definition]
r = sub, obj, act, env

[policy_definition]
p = sub, obj, act, eact

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.sub.Role == p.sub && r.obj == p.obj && r.act == p.act && r.env == p.eact
